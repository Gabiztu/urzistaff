import ClientCart from "./ClientCart";

export const metadata = {
  title: "Urzistaff — Your Cart",
  description: "Review your selection before checkout.",
};

export default function CartPage() {
  return <ClientCart />;
}
